version: "2"

linters:
  default: standard
  enable:
    - copyloopvar
    - errorlint
    - misspell
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/erikhoward/mcp-keycloak
